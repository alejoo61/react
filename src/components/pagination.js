import React, { Component } from 'react'

export class pagination extends Component {
    render() {
        return (
            <div>
                pagination
            </div>
        )
    }
}

export default pagination

pagination